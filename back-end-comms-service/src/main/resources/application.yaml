server:
  port: ${PORT:8082}

spring:
  application:
    name: comms-service

    ##Rabbitmq configuracion
  RabbitMQ:
    host: ${RABBITMQ_HOST:localhost}
    port: ${RABBITMQ_PORT:5672}
    username: ${RABBITMQ_USER:admin}
    password: ${RABBITMQ_PASSWORD:admin}
    virtual-host: ${RABBITMQ_VHOST:/}

    ##Configuracion  mail


  mail:
    host: smtp.sendgrid.net
    port: ${PUERTO_SENDGRIP:587}
    username: apikey
    password: ${SENDGRID_API_KEY}
    properties:
      mail:
        transport:
          protocol: smtp
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          ssl:
            trust: smtp.sendgrid.net

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka/}
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: ${EUREKA_PREFER_IP:true}
    hostname: ${EUREKA_HOSTNAME:localhost}

management:
  endpoints:
    web:
      exposure:
        include: health,info

## RabbitMQ Event Configuration
rabbitmq:
  queue:
    mails:
      order-created: mail.orden.creada
  exchange:
    orders: pedido.creado
  routing:
    key:
      order:
        created: order.created

gemini:
  api-key: ${GEMINI_API_KEY}

# Configuración de notificaciones por correo
email:
  notification:
    to: ${NOTIFICATION_EMAIL:unir.supplies@gmail.com} # Cambia esto por el correo del destinatario
    from: ${FROM_EMAIL:UNIR Supplies - Tu tienda de suministros <unir.supplies@gmail.com>}
